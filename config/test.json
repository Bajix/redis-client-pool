{
  "redis" : {
    "utility" : "redis://localhost:6379",
    "session" : "redis://localhost:6379",
    "kue" : "redis://localhost:6379?database=2",
    "sock": "/tmp/redis.sock",
    "options": {
      "detect_buffers": true
    }
  }
}